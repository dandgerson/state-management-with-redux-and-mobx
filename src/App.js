import Kanban from "./components/Kanban/Kanban";
import normalizedState from "./normalized-state";

const App = () => {
  return (
    <div className="app">
      <Kanban />
    </div>
  );
};

export default App;
